const LIME = Object.freeze({
  main: '#E0FF00',
  background: '#FDFEF3',
})

const GREY = Object.freeze({
  text: '#979797',
  background: '#F6F6F6',
})

const BLACK = Object.freeze({
  main: '#121111',
})

const WHITE = Object.freeze({
  main: '#fff',
})

export {
  LIME,
  BLACK,
  WHITE,
  GREY,
}